spring:
  application:
    name: shell

  main:
    web-application-type: none

  shell:
    interactive:
      enabled: true
    history:
      enabled: true
      name: .history
    script:
      enabled: true

api-gateway-url: ${API_GATEWAY_URL:http://localhost:8080}